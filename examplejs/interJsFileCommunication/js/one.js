//window.addEventListener('load', function() {

// Don't use the load listener
var namespace = {
    oneFun : function() {
        document.getElementById("one").innerHTML = "1"; 
    }
}

// }); // window.addEventListener('load', function() {
